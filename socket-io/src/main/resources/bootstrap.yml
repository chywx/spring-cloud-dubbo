spring:
  profiles:
    active: @profile.name@
  application:
    name: cloud-socket-io
  main:
    allow-circular-references: true
  cloud:
    nacos:
      discovery:
        server-addr: @config.server-addr@